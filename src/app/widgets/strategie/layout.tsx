import type { Metadata } from "next";
import "./styles.css";

export const metadata: Metadata = {
  title: "DDT31 – Stratégie",
};

export default function StrategieLayout({ children }: { children: React.ReactNode }) {
  return children;
}
